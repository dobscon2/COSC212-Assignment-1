var showReviews = (function() {
    "use strict";
    var pub = {};

    var reviews;

    pub.setup = function() {
        // setup reading reviews from reviews.json
    }

    return pub;

}());

$(document).ready(showReviews.setup);